<!DOCTYPE html>
<html>
    <%- include("head") %>
</head>

<body>
    <%- include("navbar") %>

    <div class="container">
        <div class="d-flex flex-wrap">
            <% coches.forEach(car => {%>
              <%- include("card", {car}) %>
            <% }); %>

        </div>
        

        
    </div>

    <div class="p-2 text-center boton">
        <!--Faltaria aqui el formulario-->
        <button class="btn btn-primary text-align-center" type="submit">Añadir Coche</button>
    </div>

</body>

</html>